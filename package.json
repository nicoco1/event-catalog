{
  "name": "empty-eventcatalog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eventcatalog dev",
    "build": "eventcatalog build",
    "start": "eventcatalog start",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:eventcatalog": "eventcatalog-linter",
    "lint:eventcatalog:verbose": "eventcatalog-linter --verbose",
    "update:preview": "npx npm-check-updates -u",
    "update:apply": "npx npm-check-updates -u && npm install"
  },
  "dependencies": {
    "@eventcatalog/core": "^3.28.1"
  },
  "devDependencies": {
    "@eventcatalog/linter": "^1.0.5"
  }
}
